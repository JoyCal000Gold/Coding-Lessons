{
  "name": "Quarto Login Demo",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "settings": {
    "terminal.integrated.profiles.linux": {
      "bash": {
        "path": "bash",
        "args": ["-l"]
      }
    },
    "terminal.integrated.defaultProfile.linux": "bash"
  },
 "postCreateCommand": "chmod +x /workspaces/Coding-Lessons/.devcontainer/check_versions.sh && /opt/conda/bin/conda init bash && echo 'conda activate quarto-env && cd /workspaces/Coding-Lessons && ./.devcontainer/check_versions.sh' >> ~/.bashrc",
  "forwardPorts": [5000],
  "remoteUser": "vscode",
  "features": {
    "ghcr.io/devcontainers/features/conda:1": {
      "environmentYaml": ".devcontainer/environment.yml"
    }
  }
}
